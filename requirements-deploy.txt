# Runtime-only dependencies for the deployed forecaster demo.
# Excludes Prophet, langchain, chromadb, sentence-transformers, anthropic, etc.
# Used by the Hugging Face Spaces Docker image. Use requirements.txt for development.

fastapi>=0.110
uvicorn[standard]>=0.27
streamlit>=1.30
xgboost>=2.0
scikit-learn>=1.4
pandas>=2.0
numpy>=1.24
